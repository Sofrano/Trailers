//
//  APITest.swift
//  MovieDBTests
//
//  Created by Dmitriy Safarov on 22/04/2019.
//  Copyright © 2019 SimpleCode. All rights reserved.
//

import Foundation
import XCTest
@testable import MovieDB


class NetworkTests: XCTestCase {
    
    /*
    let service = DiscoverNetworkService()
    
    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }
    
    override func tearDown() {
        super.tearDown()
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }
    
    func testGetMovieDetails() {
        
        let exp = expectation(description: "GetMovieDetails")
        
        let params = DiscoverMediaParameters()
        service.getDiscoverMedia(params)
            .done { (media) in
                exp.fulfill()
            }
            .catch { (error) in
                XCTFail("waitForExpectationsWithTimeout errored: \(error.localizedDescription)")
        }
        
        // Wait for timeout
        
        waitForExpectations(timeout: 5) { error in
            if let error = error {
                XCTFail("waitForExpectationsWithTimeout errored: \(error)")
            }
        }
    }
 */
    
}
